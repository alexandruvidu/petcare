/**
 * Here you can add more routes as constant to be used for routing within the application.
 */
export enum AppRoute {
  Index = "/",
  Login = "/login",
  Register = "/register",
  Users = "/users",
  UserFiles = "/user-files",
  Pets = "/pets",
  SitterProfiles = "/sitter-profiles",
  Bookings = "/bookings",
  Reviews = "/reviews",
  Feedback = "/feedback",
}
